{
  "name": "serverless01",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "scripts": {
    "frontend:dev": "npm run dev --workspace=apps/frontend",
    "frontend:build": "npm run build --workspace=apps/frontend",
    "cdk:deploy": "cd infra && cdk deploy -f --require-approval never",
    "cdK:destroy": "cd infra && cdk destroy -f --require-approval never",
    "lambda-layer:build-util": "npm run build --workspace=@sff/util-lambda-layer",
    "lambda-layer:watch-util": "npm run watch --workspace=@sff/util-lambda-layer",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "workspaces": [
    "apps/*",
    "infra",
    "packages/**"
  ],
  "devDependencies": {
    "@types/dotenv": "^6.1.1",
    "@types/node": "^24.3.1"
  },
  "dependencies": {
    "dotenv": "^17.2.2"
  }
}
